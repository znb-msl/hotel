package com.reservation;

// Enum representing different types of hotel rooms
public enum RoomType {
    // A standard suite with basic amenities
    STANDARD_SUITE,
    // A junior suite with additional space or features
    JUNIOR_SUITE,
    // A master suite with premium features and the most space
    MASTER_SUITE
}
